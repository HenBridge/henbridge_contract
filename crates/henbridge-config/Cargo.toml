[package]
name = "henbridge-config"
version = "0.1.0"
edition = "2021"
description = "Shared loader for HenBridge network config (config/networks.toml)"
license = "MIT"
repository = "https://github.com/HenBridge/henbridge_contract"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "1.1"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3.10"
